

```text
OmniverseCORE/
├── core/               ← API, IA, modules internes
├── dashboard/          ← TON DASHBOARD (React + TS + Tailwind)
├── scripts/            ← Installers, outils système
├── config/             ← Config globale (API, dashboard, env)
├── docs/               ← Documentation
├── assets/             ← Logos, backgrounds, SVG globaux
├── security/           ← Policies, security layer
├── .github/            ← CI/CD workflows
│   └── workflows/
│       └── ci.yml
├── .env                ← Variables globales (API_URL, PORT, etc.)
├── README.md           ← Présentation globale
├── ROADMAP.md          ← Vision & versions
├── CHANGELOG.md        ← Historique
├── cliff.toml          ← Changelog generator
└── package.json        ← (OPTIONNEL) si tu veux unifier Node
```
